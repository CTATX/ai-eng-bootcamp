# Copy to .env and fill in your key. Never commit .env or paste keys in chat.
# Format MUST include the equals sign:
OPENAI_API_KEY=sk-proj-your-key-here

# Shop intelligence P1 is synthetic Porsche and does not need a ShopMonkey key.
# SHOPMONKEY_API_KEY=  # later, live ingest only
# SHOP_INTEL_DB_PATH=data/shop_intel.sqlite3
