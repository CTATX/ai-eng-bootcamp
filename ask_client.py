"""DEPRECATED — use: streamlit run app.py → sidebar → Cost Estimator or Bootcamp Q&A."""

import streamlit as st

st.warning("This file is old. Stop this app and run:")
st.code("streamlit run app.py", language="bash")
st.markdown("Then use the **sidebar** to pick **Cost Estimator** or **Bootcamp Q&A**.")
