# Zyren

Augmented Reality companion for the service bay (holding folder).

Consumes read-only context from:

- **shop/** — vehicle, job, history briefing
- **zyteshelf/** — parts on shelf, bin, stock state
- **ferdai/** — hypothesis, procedure slots, guardrails

Does not call ShopMonkey live. Does not replace Jake (human approval gate).
